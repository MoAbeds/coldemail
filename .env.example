# ─── Database ────────────────────────────────────────────
DATABASE_URL="postgresql://user:password@localhost:5432/coldclaude?schema=public"

# ─── NextAuth ────────────────────────────────────────────
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret-here"

# ─── Google OAuth (sign-in) ──────────────────────────────
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ─── SMTP (for verification & password reset emails) ─────
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
SMTP_FROM="ColdClaude <your-email@gmail.com>"

# ─── Encryption (generate with: openssl rand -hex 32) ────
ENCRYPTION_KEY="your-32-byte-hex-encryption-key"

# ─── Google OAuth for email account connections ──────────
# Optional: separate from sign-in OAuth. Falls back to above.
GOOGLE_EMAIL_CLIENT_ID=""
GOOGLE_EMAIL_CLIENT_SECRET=""

# ─── Microsoft OAuth for Outlook connections ─────────────
MICROSOFT_CLIENT_ID=""
MICROSOFT_CLIENT_SECRET=""

# ─── Redis (for BullMQ job queue) ────────────────────────
REDIS_URL="redis://localhost:6379"

# ─── Sentry (error tracking) ────────────────────────────
SENTRY_DSN=""
SENTRY_AUTH_TOKEN=""

# ─── Cron Security ──────────────────────────────────────
# Shared secret to protect cron endpoints (generate with: openssl rand -hex 16)
CRON_SECRET=""

# ─── Deployment ─────────────────────────────────────────
NODE_ENV="development"
